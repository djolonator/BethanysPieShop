@model PieManagerVM

<h1>Pie manager</h1>  <p class="button">
    <a class="btn btn-primary"
       asp-controller="Pie"
       asp-action="CreatePie"
       asp-route-Category="">Create pie</a>
</p>

@foreach (var pie in Model.Pies)
{
    <partial name="_PieManager" model="pie" />
}